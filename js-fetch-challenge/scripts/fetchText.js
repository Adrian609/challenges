ready().then(() => {

  function choosePage(page) {
    let pageNumber = page.getElementById();
  }

  pages = getElementByTagName('<li>')
  pages.forEach do |page|
    page.addEventListener('click', page;
  end
  debugger

});
